import GenerateImage from "@/components/generateImage/GenerateImage";

export default function Home() {
  return (
    <main className="w-full h-fit min-h-full flex flex-col items-center justify-between p-6">
      <GenerateImage />
    </main>
  );
}
